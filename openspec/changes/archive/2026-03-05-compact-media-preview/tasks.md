## 1. CSS 尺寸调整（ScriptItem.vue）

- [x] 1.1 将 `.script-media-images.single .script-media-img` 的 `max-height` 由 `200px` 改为 `120px`
- [x] 1.2 将 `.script-media-images.grid .script-media-img` 的 `height` 由 `120px` 改为 `80px`
- [x] 1.3 为 `.script-media-video` 新增 `max-height: 160px`

## 2. 验证

- [ ] 2.1 在应用中查看含图片话术，确认单图高度正常缩小
- [ ] 2.2 在应用中查看含多图话术，确认 grid 高度正常缩小
- [ ] 2.3 在应用中查看含视频话术，确认视频高度被限制
